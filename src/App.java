 import java.util.Scanner;
 public class App {
    public static void main(String[] args) throws Exception {
        System.out.println("Hello, World!"); 

        //booleano (falso,verdadeiro)
        boolean morango= true; boolean morango= false;
        //scanner(entrada de dados)
        Scanner scanner = ()
        //int ( numero inteiro) (int numero_inteiro = 10;)
        //double(numero quebrado) (double temperaturaCelsius = scanner.nextDouble();)
        // if (temperaturaCelsius < zeroAbsolutoEmCelsius)
        //else()
        // / ( dividir ) + (mais) -(menos) *(multipricar )
        // && ( compra ) || (compara)
        // a 5 = b 10  
        // (a) é igual a (b) ? se não então sempre sera false     
        //char (caractere) 'a'

    }
}
